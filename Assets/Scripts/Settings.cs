using UnityEngine;
using System.Collections;
using UnityEngine.Audio;

public class Settings : MonoBehaviour {
	

public void Difficulty(float ballSpeed) {
		Ball.SetDifficulty (ballSpeed);
	}

public void MusicManager () {
	}
}
